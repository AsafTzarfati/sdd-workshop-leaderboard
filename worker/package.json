{
  "name": "drone-watchdog-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create LEADERBOARD",
    "kv:list": "wrangler kv key list --binding=LEADERBOARD",
    "kv:reset": "wrangler kv key delete --binding=LEADERBOARD submissions"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
